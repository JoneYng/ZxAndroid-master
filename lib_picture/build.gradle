apply from: "../lib.build.gradle"

android {

}
def apiPicture = rootProject.ext.picture

dependencies {
    
    api deps.glide
    kapt deps.glide_compiler
    api deps.glide_transformations

    api deps.android_material
    //图片选择库
    apiPicture.each { k, v -> api v }

}
